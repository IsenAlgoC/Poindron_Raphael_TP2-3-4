#include <stdlib.h>
#include <stdio.h>

int main() {

	int A = 0, B = 0, C = 0;
	scanf_s("%d", &A);
	scanf_s("%d", &B);
	scanf_s("%d", &C);
	if (A <= 55, B <= 35, C <= 25) { printf("VALIDE");}
	else { "NON VALIDE"; }
}
